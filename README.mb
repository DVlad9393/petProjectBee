Project setup:

pip install virtualenv
virtualenv venv
source venv/bin/activate

pip install -r requirements.txt

Starting auto tests:
python -m pytest

Starting auto tests with allure:
python -m pytest --alluredir=./allure-results
allure serve